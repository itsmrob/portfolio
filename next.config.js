/** @type {import('next').NextConfig} */
const nextConfig = {
    // exportPathMap: async () => {
    //     return {
    //         '/': { page: '/index.js'} 
    //     }
    // }
}

module.exports = nextConfig
